import Image from "next/image"; import { Shell } from "@/components/shell"; import { gallery } from "@/lib/content";
export default function Galeria(){return <Shell><main><section className="page-hero"><div className="shell"><p className="eyebrow">Galeria</p><h1>Em movimento</h1></div></section><section className="section"><div className="shell gallery">{gallery.map(([image,caption])=><figure key={image}><Image src={image} alt={caption} width={900} height={900}/><figcaption>{caption}</figcaption></figure>)}</div></section></main></Shell>}