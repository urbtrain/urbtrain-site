"use client";import{useState}from"react";import Link from"next/link";import{Shell}from"@/components/shell";import{browserSupabase,configured}from"@/lib/supabase-browser";export default function Login(){const[e,setE]=useState("");const[p,setP]=useState("");const[m,setM]=useState("");async function login(){try{const{error}=await browserSupabase().auth.signInWithPassword({email:e,password:p});setM(error?.message||"Login realizado.")}catch{setM("Configure o Supabase para ativar o login.")}}async function google(){try{await browserSupabase().auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}})}catch{setM("Configure o Supabase para ativar o Google.")}}return <Shell><main className="shell section"><p className="eyebrow">Acesso</p><h1>ENTRAR</h1><div className="form"><input placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input type="password" placeholder="Senha" value={p} onChange={x=>setP(x.target.value)}/><button className="button" onClick={login}>Entrar</button><button className="button alt" onClick={google} disabled={!configured}>Entrar com Google</button><p>{m}</p><Link href="/cadastro">Criar conta</Link></div></main></Shell>}