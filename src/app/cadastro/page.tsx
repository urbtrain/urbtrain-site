"use client";import{useState}from"react";import Link from"next/link";import{Shell}from"@/components/shell";import{browserSupabase}from"@/lib/supabase-browser";export default function Cadastro(){const[e,setE]=useState("");const[p,setP]=useState("");const[m,setM]=useState("");async function signup(){try{const{error}=await browserSupabase().auth.signUp({email:e,password:p,options:{emailRedirectTo:`${location.origin}/auth/callback`}});setM(error?.message||"Confira seu email para confirmar a conta.")}catch{setM("Configure o Supabase para ativar o cadastro.")}}return <Shell><main className="shell section"><p className="eyebrow">Comunidade</p><h1>CRIAR CONTA</h1><div className="form"><input placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input type="password" placeholder="Senha (minimo 6 caracteres)" value={p} onChange={x=>setP(x.target.value)}/><button className="button" onClick={signup}>Cadastrar</button><p>{m}</p><Link href="/login">Ja tenho conta</Link></div></main></Shell>}