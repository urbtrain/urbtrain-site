import Image from "next/image";
import { AdminGate, AdminLayout } from "@/components/admin-layout";
import { getAdminAccess } from "@/lib/admin";
import { serverSupabase } from "@/lib/supabase-server";
import { archiveGalleryItem, uploadGalleryItem } from "../actions";
export default async function GalleryPage(){const access=await getAdminAccess();if(access.state!=="admin")return <AdminGate access={access}/>;const supabase=await serverSupabase();const {data:items}=await supabase.from("gallery_items").select("id,image_path,alt_text,caption,position,published").order("position",{ascending:true});return <AdminLayout active="/admin/galeria" title="Galeria"><section className="admin-section"><div className="admin-section-head"><div><p className="eyebrow dark">Mídia</p><h2>Enviar imagem</h2></div></div><form className="admin-form" action={uploadGalleryItem}><label>Imagem<input type="file" name="file" accept="image/jpeg,image/png,image/webp" required/></label><label>Texto alternativo<input name="alt_text" maxLength={180} required/></label><label>Legenda<input name="caption" maxLength={300}/></label><label>Posição<input name="position" type="number" min="0" defaultValue="0"/></label><label className="check"><input name="published" type="checkbox" defaultChecked/> Publicar agora</label><button className="button">Enviar imagem</button></form></section><section className="admin-section"><div className="admin-section-head"><div><p className="eyebrow dark">Acervo</p><h2>Imagens cadastradas</h2></div><span>{items?.length??0} itens</span></div><div className="admin-gallery-grid">{(items??[]).map(item=><article key={item.id}><div className="admin-gallery-image"><Image src={item.image_path} alt={item.alt_text} width={500} height={500}/></div><strong>{item.caption||item.alt_text}</strong><span>Posição {item.position} · {item.published?"Publicada":"Arquivada"}</span><form action={archiveGalleryItem}><input type="hidden" name="id" value={item.id}/><button className="button admin-secondary">Arquivar</button></form></article>)}</div></section></AdminLayout>}