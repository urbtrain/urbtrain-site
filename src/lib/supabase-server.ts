import{createServerClient}from"@supabase/ssr";import{cookies}from"next/headers";const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;export const configured=Boolean(url&&key);export async function serverSupabase(){if(!url||!key)throw new Error("Supabase nao configurado");const jar=await cookies();return createServerClient(url,key,{cookies:{getAll(){return jar.getAll()},setAll(items){try{items.forEach(i=>jar.set(i.name,i.value,i.options))}catch{}}}})}